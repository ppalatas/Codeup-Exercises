<?php 

$testArray = ['e', 'a', 'g', 'c', 'i', 'd', 'f', 'b', 'h'];

function alphabeticalOrder($array){

	$alphabet = range('a', 'z');

	$newArray = array();

	if ($letters[0] < $letters[1]){
		array_push($testArray, $newArray);
		
	}

	var_dump($testArray);

	return $testArray;
}

alphabeticalOrder();

// assign the arrays letters as values to be sorted 

// if the letters value is less than the others it will be added to the new array first 

// 











 ?>